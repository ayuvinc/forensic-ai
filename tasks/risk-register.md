# RISK REGISTER

## Format
| ID | Status | Category | Risk | Impact | Mitigation |
|----|--------|----------|------|--------|------------|

---

## Risks

| ID | Status | Category | Risk | Impact | Mitigation |
|----|--------|----------|------|--------|------------|
| R-001 | OPEN | API | Tavily free tier limited to 1000 searches/month — could exhaust during heavy testing | Medium | Cache all research results locally per case_id; add `use_cached` flag to research tools |
| R-002 | CLOSED | API | Anthropic API key not yet confirmed working in this environment | High | RESOLVED: API key active, smoke test passed Session 009 (2026-04-04) |
| R-003 | OPEN | ARCH | Arabic rendering in Rich terminal may not display correctly on all Mac/Windows terminals | Low | Test early with a sample bilingual output; have plain-text fallback |
| R-004 | OPEN | SCOPE | Proposal deck (PPT prompt pack) depends on undefined external tool `claude_ppt` | Low | Keep deck output as prompt files only (no direct PPT generation); document dependency clearly in README |
| R-005 | CLOSED | PROCESS | No task tracking system existed at project start | Low | Resolved: tasks/ scaffold created 2026-03-29 |
| R-006 | CLOSED | AK_DECISION | docs/problem-definition.md and docs/scope-brief.md are stubs — no confirmed content | Medium | RESOLVED Session 036: docs/scope-brief.md fully updated (completed items marked, Sprint-IA-01/02 scope added). docs/problem-definition.md deleted (empty template — redundant with ba-logic.md). traceability-matrix.md also deleted (empty — replaced by living ba-logic.md). |
| R-007 | CLOSED | AK_DECISION | docs/hld.md has sections marked [TO VERIFY VIA /architect SESSION] — partial only | Low | RESOLVED Session 036: docs/hld.md fully rewritten — two-arc model, all 40+ components, Partner behavior, shipping models, all gaps filled. |
| R-008 | OPEN | AK_DECISION | AKR-09 deferred: AK-Cognitive-OS skills/ directory contains v1.x format commands — cannot install without downgrading project | Low | Revisit only when AK-Cognitive-OS skills/ are updated to v2.0 format |
| R-009 | MITIGATED | PROCESS | No live smoke test run — 57 modules structural only, never tested with real API keys | High | MITIGATED Session 015: P7-GATE PASSED — FRM Option 6 complete (knowledge_only, 2 modules, no crash, final_report.en.md written). Option 4 passed Session 009. Core pipeline confirmed working. Live-mode (TAVILY) run still pending but not a blocker. |
| R-010 | MITIGATED | ARCH | FRM guided exercise redesign (Sprint-10D) touches workflows/frm_risk_register.py and schemas/artifacts.py — regression risk to existing FRM pipeline if refactor is not isolated correctly | High | MITIGATED Session 032: P7-GATE baseline exists (Session 015, knowledge_only, final_report.en.md written). Condition satisfied — Sprint-10D may proceed with feature-flag isolation. |
| R-011 | OPEN | ARCH | tools/knowledge_library.py sanitisation — if PII stripping logic is incomplete, client names or passport numbers from historical reports could surface in new engagement outputs | High | Mitigation: ARCH-S-02 schema defines PII fields explicitly; sanitise() validates stripped output against known PII field list before writing index entry; SanitisationError hard gate |
| R-012 | OPEN | SCOPE | 35+ new tasks in Sprint-10A–10H represent 2–3x the volume of work already completed; scope creep risk if all tasks attempt to start simultaneously | Medium | Mitigation: critical path enforced (Schemas → KFs → Library → Workflows); next session starts with ARCH-S-01 + KF-00 only |
| R-013 | CLOSED | PROCESS | docs/hld.md still has sections marked [TO VERIFY VIA /architect SESSION] — partially addressed but not updated this session | Low | RESOLVED Session 036: docs/hld.md fully rewritten — all [TO VERIFY] stubs removed. Two-arc model, Phase 9 engagement flow, Streamlit primary, all components current. |
| R-014 | MITIGATED | ARCH | FRM workflow split (P8-02) refactors run_frm_workflow() body — risk that CLI behaviour changes or run_frm_workflow() no longer calls review loop correctly | High | MITIGATED: P8-02 completed and merged (completed-tasks.md). run_frm_workflow() signature intact. P8-02d smoke test pending (manual, low priority). |
| R-015 | MITIGATED | ARCH | Long FRM pipeline (2–3 min) with coarse on_progress calls — browser spinner may show no updates for extended periods, user assumes crash | Medium | MITIGATED Session 032: code inspection of workflows/frm_risk_register.py confirms on_progress called per agent step — lines 144 (Junior), 147 (PM), 151 (Revision), 160 (Partner) — not module-level only. |
| R-016 | CLOSED | SECURITY | Streamlit default binding is 0.0.0.0:8501 — case data (firm_profile, case reports) visible on LAN if machine is on shared network | Low | RESOLVED Session 032: `[server] address = "localhost"` added to `.streamlit/config.toml` — binding restricted at config level. |
| R-017 | OPEN | ARCH | FE-08 interim migration uses shutil.move() — not atomic; process kill mid-migration leaves mixed state (some v*.json in root, some in interim/) | Low | Mitigation: best-effort migration acceptable — artifacts still readable in both locations; case_tracker must tolerate files in either location. |
| R-018 | OPEN | PROCESS | pages/ numbering skips 1 (0_Scope, 2_Investigation…) — sidebar ordering gap is cosmetic but may confuse if pages are reordered later | Low | Mitigation: renumber pages sequentially at P8-08 task time; architect to confirm final numbering before build. |
| R-019 | CLOSED | SECURITY | P9-01 project_name → slug is a user-controlled string used as a filesystem folder name — path traversal risk if not sanitized (e.g. "../../../etc/passwd") | High | RESOLVED: Pydantic validator implemented and tested — test_project_schema.py passes 13/13 slug/traversal tests including "../../../etc/passwd" → ValueError. |
| R-020 | OPEN | ARCH | Phase 8 cases (UUID format) will not have A-F folder structure — Case Tracker and workspace pages must tolerate both formats without crash | Medium | Mitigation: P9-04b `is_af_project()` flag distinguishes formats; all page code gates A-F features on this check; legacy cases shown in separate expander in Engagements page. |
| R-NEW-01 | OPEN | ARCH | openpyxl Excel heat map uses ARGB hex for cell fills — must test against actual Excel open, not just file creation | High | Mitigation: Sprint-FR FR-04 AC must include "open in Excel and verify colour rendering" step |
| R-NEW-02 | OPEN | ARCH | DOCX TOC field requires Word to refresh on open — triggers "Update fields" prompt on macOS/Windows | Low | Mitigation: document in README; acceptable UX |
| R-NEW-03 | OPEN | ARCH | Consultant-uploaded .docx template may use styles incompatible with python-docx injection | Low | Mitigation: BaseReportBuilder falls back to default styles with logged warning |
| R-NEW-04 | CLOSED | ARCH | Investigation report has 13 sections — python-docx footnote support is limited; Exhibit N citations may need inline parenthetical references instead | High | RESOLVED Session 032: WF-01c built with inline parenthetical "Exhibit N" references in build_detailed_findings() — decision made at build time, no footnote API required. |
| R-NEW-05 | OPEN | ARCH | Confirmed-lead → draft finding generation fires Haiku API call during Input Session (outside orchestrator) — new call pattern | Medium | Mitigation: gate on RESEARCH_MODE; knowledge_only generates from model knowledge; live calls Haiku |
| R-NEW-06 | OPEN | SCOPE | DD subject linkages currently text-based — visual network diagram would require graphviz | Low | Mitigation: out of scope v1; flagged for future session |
| R-NEW-07 | OPEN | ARCH | sentence-transformers first run downloads ~90MB model from HuggingFace — fails silently offline | High | Mitigation: EMB-01 must detect download failure; fall back to full-document context (existing behavior); log warning to consultant |
| R-NEW-08 | OPEN | ARCH | ChromaDB may slow on large document sets — batch embedding on upload (not on-demand retrieval) required | Medium | Mitigation: Sprint-EMB EMB-02 embeds at registration time; already in design |
| R-NEW-09 | OPEN | ARCH | AIC-02 pre-final-run Sonnet call reads case_intake.md — large files may approach context limit | Medium | Mitigation: load only last 10 entries from case_intake.md + full key_facts/red_flags; open leads in full |
| R-NEW-10 | OPEN | ARCH | HybridIntakeEngine (IA-02-C2d) makes Claude API call during intake — slow API or timeout hangs intake form | Medium | Mitigation: knowledge_only fallback designed into IA-02-C2d — skips API call, appends remarks as text note. Timeout guard: use existing `MAX_API_RETRIES` + `TIMEOUT` constants from BaseAgent |
| R-NEW-11 | OPEN | ARCH | Multi-step Streamlit session state in HybridIntakeEngine — if user navigates away mid-intake, partial state persists in session_state and may load corrupted on return | Low | Mitigation: namespace all session_state keys by `f"hybrid_intake_{workflow_id}_{step}"`. Add `reset_intake()` method called on page load if step == "done" from prior run |
| R-NEW-12 | OPEN | ARCH | `investigation_type` field addition to `CaseIntake` (IA-02-B2b) — existing pipeline code reading `intake.description` to infer investigation type may behave differently when a structured field is present | Medium | Mitigation: run 131 tests after IA-02-B2b; inspect any test that references investigation_type parsing; add explicit field read in agent prompts after IA-02-B2b merges |
| R-NEW-13 | OPEN | ARCH | Evidence-chain enforcement is prompt-enforced, not runtime-enforced — bad evidence IDs and non-permissible evidence are not blocked by post-hooks or partner path; confirmed by Codex empirical run 2026-04-23 | High | Mitigation: inspect partner path, post-hooks, and evidence classifier before production use; add runtime block to validate_schema post-hook; do not treat empirical suite as closed quality gate until this is resolved. Targeted sprint required before any production case delivery. |
